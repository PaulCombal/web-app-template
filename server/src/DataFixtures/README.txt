The FX_ prefix is used to load the fixtures ina certain order, since it looks like Symfony uses the alphabetical order to load them.
put google UIDs in google_subs.txt to make them admin and all on fixture load.
